<%= form_for(@meeting) do |f| %>
  <% if @meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
      <% @meeting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :what %><br />
    <%= f.text_area :what %>
  </div>
  <div class="field">
    <%= f.label :proposed_date_for_meeting %><br />
    <%= f.datetime_select :proposed_date_for_meeting  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
